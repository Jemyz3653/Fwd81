// SPDX-License-Identifier: LGPL-2.1-or-later
//
// Инструментовка загрузчика (ТОЛЬКО ЧТЕНИЕ, эксперимент вехи M3).
//
// Задача: под IFEO увидеть в журнале, какую именно функцию загрузчика дёргает
// разрешение статических импортов при провале tests/synthetic — экспортируемую
// или внутреннюю, и через пролог или по смещению. Это подтверждает гипотезу
// статического анализа (см. architecture.md) и выбирает слой будущего перехвата.
//
// Способ — аппаратные точки останова (debug-регистры DR0..DR3) + тощий
// обработчик исключений. КОД НЕ ПАТЧИТСЯ ни на байт: DR-регистры лишь поднимают
// исключение при исполнении по адресу. Патч памяти (сам перехват) — отдельная
// задача, после этого опыта.
//
// ВНИМАНИЕ: это временная инструментовка под конкретную сборку ntdll (см.
// адреса в .c), включённая на время M3. Продакшн-перехват будет находить адреса
// в рантайме иначе.

#ifndef FWD81_PROBE_LDR_H
#define FWD81_PROBE_LDR_H

// Один раз: зарегистрировать обработчик, прогнать самопроверку и — если ntdll
// той сборки, под которую сняты адреса — расставить точки останова на функциях
// загрузчика на ТЕКУЩЕМ потоке. Безопасно звать повторно (сработает единожды).
void Fwd81LdrProbeArm(void);

#endif // FWD81_PROBE_LDR_H
