// SPDX-License-Identifier: LGPL-2.1-or-later
//
// Резолвер подстановки — общая логика для ЛЮБОГО механизма перехвата.
//
// Отвечает на два вопроса, не зная, каким способом его позвали (thunk
// провайдера / прямой хук / переписывание таблицы импорта — выбор ещё впереди):
//   * обрабатываем ли мы отсутствующую библиотеку с таким именем;
//   * есть ли у нас подставная функция с таким именем.
//
// Правило (из architecture.md): вмешиваемся ТОЛЬКО когда оригинал вернул отказ.
// Сам факт отказа проверяет вызывающая обёртка; резолвер лишь решает, беремся ли
// мы за это имя, и отдаёт подстановку.
//
// ОБЛАСТЬ M3: список покрытия здесь — тестовый (имена из tests/synthetic), чтобы
// проверить сам механизм перехвата. Настоящая интеграция со списком missing и
// таблицей покрытия (как в fwd81diag) придёт с реализациями функций в M4.

#ifndef FWD81_RESOLVE_H
#define FWD81_RESOLVE_H

#include <windows.h>

// Обрабатываем ли отсутствующую библиотеку dll (UTF-16, только имя файла).
BOOL Fwd81HandlesMissingDll(const wchar_t *dll);

// Адрес нашей подставной функции для имени name (ASCII) или NULL, если не наше.
void *Fwd81ResolveProcedure(const char *name);

#endif // FWD81_RESOLVE_H
